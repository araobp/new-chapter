# 最後の年の仕事の進め方

ここ3年以上、Jupyter Notebookを活用したマーケティングデータ分析業務を担当してきました。本READMEでは、AIエージェントとデータ分析基盤を高度に連携させ、試行錯誤の末にたどり着いた **「最後の仕事のやり方」** ―― 最も生産性が高く、軽快で仕事が楽しくなる分析ワークフローの構成とノウハウをまとめています。

## 背景と課題（Why）

当初はClaude Cowork単体にデータ分析作業全般を任せる運用を試みましたが、規模の拡大に伴い以下の課題に直面しました。

* **`SKILL.md` の肥大化と誤動作の増加**  
  自然言語による指示、処理フローやコンテキスト（`SKILL.md`）が肥大化・複雑化するにつれ、AIが指示文を正確に解釈できなくなり、コンテキストの誤認識やコード生成の不具合が頻発しました。
* **TAT（Turn Around Time）の長期化**  
  一連の分析処理を完了するまでに数分以上の待ち時間が発生し、トライ＆エラーのサイクルが停滞。結果として業務効率がかえって悪化してしまいました。
* **出力結果に対する信頼性の問題**  
  Claude Cowork単体で分析を完結させると内部の処理プロセスが見えにくく、提示された集計結果や分析コードが本当に正しいのかどうか直接確認できないため、結果を信用しきれないという問題がありました。
* **データサイエンスリテラシーが共有できない共同作業のストレス（エンジニアと非エンジニアによる共同作業）**  
  Claude Coworkという同じ土俵でありながら、前提となる知識やリテラシーが共有できていない中での共同作業は、大きなストレスとなっていました。

## ソリューション：役割分離によるプロセスの最適化（How）

上記の課題を解決するため、 **「論理と思考（AI）」** と **「計算と実行（コード基盤）」** の切り離しに加え、 **「データ分析者」** と **「結果利用者」** の役割およびインターフェースを明確に分離するアーキテクチャへと移行しました。

Jupyter Notebook MCPを導入することで、AIが生成したコードや対話的な実行結果・中間データがすべてノートブック上に可視化されます。 **データ分析者はコードや実行プロセスを直接確認・検証できるため、分析結果に対する信頼性が飛躍的に向上しました。**

データ収集から集計・構造化までのフローをJupyter Notebook上で完結させ、成果物をSQLite DB、自動化スクリプト、および軽量な `SKILL.md` に集約することで、分析基盤の複雑さを意識させずに利用者が結果を活用できる環境を整え、劇的な改善を達成しました。

### コンポーネントと役割分担

```text
                             外部データソース (Web / API / CSV)
                                               │
                                               ▼
┌─────────────────┐       MCP       ┌──────────────────────┐
│  Claude Cowork  │ ───────────────>│ Jupyter Notebook MCP │
│ (Data Scientist)│                 │ (Scraping / LLM API /│
└─────────────────┘                 │  CSV -> SQLite DB)   │
                                    └──────────┬───────────┘
                                               │
                                               ▼
┌─────────────────┐       Shared     ┌──────────────────────┐
│  Box / SKILL.md │ <─────────────── │ SQLite DB / Scripts  │
│ (Context/Share) │                  │ (Final Output)       │
└─────────────────┘                  └──────────────────────┘
```

| 構成要素 | 役割・設計方針 |
| :--- | :--- |
| **Claude Cowork** | **優秀なデータサイエンティスト**<br>分析アプローチの立案、コード生成、結果の解釈を担当。 |
| **Jupyter Notebook MCP** | **データ収集・処理・分析実行基盤**<br>Claudeが生成したコードを受け取り、Pythonカーネル上で高速・確実に対話実行。<br>・**CSVインポート**: 既存のローカル/外部CSVデータの取り込みと整形<br>・**Webスクレイピング**: 外部サイトからの最新トレンドデータ等の自動収集<br>・**より低コストなLLM APIの活用**: 非構造化テキストの要約・分類・感情分析処理<br>・**処理ステップの最終確認**: 各セルのコードを読み、グラフでの処理結果図示も参考に処理の正当性を確認 |
| **SQLite DB** | **構造化データの最終成果物**<br>スクレイピング結果、LLM処理結果、CSVデータを集約・構造化し、単一のSQLite DBとして出力。 |
| **配布用スクリプト** | **汎用・自動化ツール（`.py`）**<br>Jupyter Notebook上で構築・検証した処理フローから、第三者が軽量 `SKILL.md` 経由で直接実行・呼び出し可能なPythonスクリプトを出力・提供。 |
| **Box** | **データ共有ストレージ**<br>作成されたSQLite DB、配布用スクリプト、および後続利用のためのドキュメントをチーム間で迅速に同期。 |
| **軽量な `SKILL.md`** | **コンテキストの軽量化**<br>`SKILL.md` には最小限の手順とルールのみを記述。指示文の過度な肥大化を防ぎ、Claude Coworkの応答速度と精度を最大化。 |

## 分析ワークフロー（Pipeline）

Jupyter Notebook上での具体的なデータ処理パイプラインは以下の流れで進行します。

1. **マルチソース・データ収集（Data Ingestion）**
   * **Webスクレイピング**: BeautifulSoup / Playwright 等を用いて外部Webデータを取得。
   * **CSV取り込み**: パンダス（pandas）等を用いて既存のCSVファイルを読み込み、初期クレンジングを実行。
2. **LLM APIによる高度なテキスト加工（AI Enrichment）**
   * スクレイピング文面やCSV内の自由記述テキストに対し、LLM API（Gemini API / OpenAI API / Anthropic API等）をNotebookから非同期呼び出し。
   * カテゴリ分類、感情分析、重要キーワード抽出などの非構造化データの構造化処理を実行。
3. **SQLite DBへの統合・格納（Storage Integration）**
   * 収集・加工した全データをリレーショナル構造として整列させ、SQLite DB（`.db`）へワンストップで書き込み。
4. **配布用スクリプトの出力（Script Export）**
   * 検証済みのデータ抽出・分析ロジックをNotebookからクリーンな標準Pythonスクリプト（`.py`）として切り出し、第三者が再利用しやすい形へモジュール化。

## 成果物（Deliverables）

本ワークフローの実行によって生成・維持される主要な成果物は以下の通りです。

1. **Jupyter Notebook（`.ipynb`）**  
   スクレイピング、LLM API呼び出し、CSV加工、DB格納までの一連の再現性を確保したパイプラインコードと視覚化グラフ。
2. **SQLite Database（`.db`）**  
   Web/CSV/LLM加工データが統合格納されたポータブルなデータベース。後続のBIツール連携や再利用が容易。
3. **配布用Pythonスクリプト（`.py`）**  
   データ抽出・クエリ実行・自動集計用に最適化されたスタンドアロンスクリプト。Notebook環境なしで直接実行が可能。
4. **活用向け `SKILL.md`**  
   上記DBや配布用スクリプトを第三者や他のAIエージェントがスムーズに再利用・実行するための、軽量化されたプロンプト/仕様書。

## 前提条件・導入（Prerequisites）

本環境を再現するには、以下の環境およびMCP（Model Context Protocol）サーバーが必要です。

* **[Jupyter MCP Server](https://jupyter-mcp-server.datalayer.tech/)**  
  Jupyter Notebook環境とClaudeを接続し、セル単位でのコード実行や状態取得を可能にします。
* **Box MCP Server**  
  クラウドストレージへの成果物（DB、Notebook、スクリプト）の保存および同期を自動化します。
* **Python実行ライブラリ / API Key**  
  スクレイピング用ライブラリ（`requests`, `beautifulsoup4` など）、データ操作用（`pandas`, `sqlite3`）、およびLLM API（`openai`, `anthropic` など）のアクセスキー。

## 導入効果

* **レスポンスと処理速度の劇的な向上**: 自然言語指示の最小化と実行基盤の分離により、TATが大幅に短縮。
* **データ収集からDB化までの自動一貫処理**: Web・CSV・LLM加工テキストがNotebook内でスムーズにSQLite DBへ集約可能。
* **第三者へのスムーズな引き継ぎと自動化**: 検証済みスクリプトと軽量 `SKILL.md` の組み合わせにより、非エンジニアや他エージェントでも確実にロジックを再利用可能。
* **処理精度の安定化**: 各ステップの実行結果やAPI応答をステップバイステップで確認・修正できるため、意図しない誤動作やエラーを防止。
* **軽快で楽しい作業体験**: ストレスが大幅に軽減され、AIとの高度な協調によって仕事の楽しさとスピード感を両立。

## 補足：生成AI依存が及ぼす認知・思考への懸念

高度なAI連携を実現する一方で、AIへの過度な依存がもたらす認知面やエンジニアリング上のリスクにも留意が必要です。

* **達成感の欠如と疲労**: 時間をかけて作業しても技術を極めた実感が得にくく、疲労やストレスばかりが残る傾向があります。
* **現場知見の喪失**: 本質的な知能やノウハウは、ローレベルな現場での泥臭い試行錯誤の中で培われます。
* **基礎力の未熟化**: 多くの専門家が指摘するように、まずはAIを使わず自力で基礎問題を解く経験を経なければ、思考力が養われず将来は不利を被ります。
